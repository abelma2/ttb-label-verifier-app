# --- Secrets: never commit -------------------------------------------------
# Local OpenAI key lives in .secrets/secrets.toml (sourced into OPENAI_API_KEY).
.secrets/
.streamlit/          # legacy name, kept ignored defensively
.env
.env.*
!.env.example

# --- Python ------------------------------------------------------------------
__pycache__/
*.py[cod]
.pytest_cache/
.audit-venv/
.venv/
venv/

# --- Node / Next.js ----------------------------------------------------------
node_modules/
.next/
out/
.vercel/
*.tsbuildinfo
next-env.d.ts

# --- Run artifacts & scratch ---------------------------------------------------
output/
archive/
artifacts/

# --- Dev tooling & evidence: kept LOCAL-ONLY on main (full tracked set lives on
# the dev-archive branch). The repo's main branch carries only what a user needs
# to download and run the app. Locally it all lives under private/.
private/
test_labels/
BENCHMARK_NOTES.md
CLAUDE.md
AGENTS.md
app.py
requirements-streamlit.txt
docs/
eval/
scripts/
adversarial/
bold_safety/
supporting_docs/

# --- Local tooling state ---------------------------------------------------------
.claude/

# --- OS noise ------------------------------------------------------------------
.DS_Store
Thumbs.db
# Excel owner-lock files (left behind while a workbook is open in Excel)
**/~$*
